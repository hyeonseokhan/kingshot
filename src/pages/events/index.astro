---
import GuideLayout from '@/layouts/GuideLayout.astro';
import {
  getGuidesByCategory,
  getEnIndexByCategory,
  getGuideEn,
  entryToSlug,
  buildNavItems,
} from '@/lib/guides';

const guides = await getGuidesByCategory('events');
const enIndex = await getEnIndexByCategory('events');
const first = guides[0];
if (!first) {
  throw new Error('events 카테고리에 가이드가 없습니다.');
}
const firstSlug = entryToSlug(first);
const { Content: ContentKo, headings: headingsKo } = await first.render();

const enEntry = await getGuideEn(first);
const enRendered = enEntry ? await enEntry.render() : null;
const ContentEn = enRendered?.Content;
const headingsEn = enRendered?.headings ?? [];

const navItems = buildNavItems(guides, 'events', firstSlug, enIndex);
---

<GuideLayout
  title={`${first.data.title} — PNX 연맹`}
  description="PNX 연맹 이벤트 가이드"
  navHeader="목차"
  navHeaderKey="common.contents"
  navItems={navItems}
  headings={headingsKo}
  hasEn={!!ContentEn}
  headingsEn={headingsEn}
>
  {ContentEn ? (
    <>
      <ContentKo slot="ko" />
      <ContentEn slot="en" />
    </>
  ) : (
    <ContentKo />
  )}
</GuideLayout>
