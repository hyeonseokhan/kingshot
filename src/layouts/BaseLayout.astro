---
import '@/styles/global.css';
// 모든 페이지 공통 컴포넌트 (.btn, .page-intro, .members-header, .manage-page,
// 모달, player-card, 빈 상태 셀 등) — 페이지가 manage.css 등을 import 안 해도
// 공통 시각 패턴은 보장됨.
import '@/styles/components.css';
import Header from '@/components/Header.astro';
import MobileNav from '@/components/MobileNav.astro';
import AuthDialog from '@/components/AuthDialog.astro';

interface Props {
  title?: string;
  description?: string;
}

const { title = '연맹 가이드', description = 'PNX 연맹 가이드' } = Astro.props;
---

<!doctype html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16.png" />
    <link rel="icon" type="image/png" sizes="96x96" href="/favicon-96.png" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="preconnect" href="https://cdn.jsdelivr.net" crossorigin />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable-dynamic-subset.min.css"
    />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500&display=swap"
    />
    <!-- 권한 가드 동기 적용 — module script(boot 지연) 이전에 inline script 로 즉시 dataset 세팅.
         Header/MobileNav 의 [data-require-kingshot-id] 가시성을 CSS 가 첫 paint 부터 결정 → 깜박임 0. -->
    <script is:inline>
      (function () {
        try {
          var s = JSON.parse(localStorage.getItem('tileMatchAuth') || 'null');
          if (s && s.player_id) document.documentElement.dataset.kingshotId = s.player_id;
        } catch (e) {}
      })();
    </script>
    <script>
      import '@/scripts/legacy-hash-redirect';
      import { bootI18n } from '@/i18n';
      bootI18n();
    </script>
  </head>
  <body>
    <Header />
    <MobileNav />
    <AuthDialog />
    <slot />
  </body>
</html>
